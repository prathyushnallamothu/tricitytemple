to run backend install Golang and run tricity.go file in backend 
``` console 
go run tricity.go
```

to run any frontend project run (install vuejs and npm before this step)
``` console
npm install
npm run serve
```
